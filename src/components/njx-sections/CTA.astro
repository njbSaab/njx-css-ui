<section class="lib-section" id="cta-blocks">
    <div class="lib-section-title">⚡ CTA Blocks</div>
    <div class="lib-section-desc">
        Ready call-to-action sections for product pages, docs and newsletters.
    </div>

    <div class="lib-component">
        <div class="lib-component-label">Gradient CTA</div>
        <div class="lib-preview cta-block-preview">
            <section class="rcta rcta-gradient card-gradient">
                <div>
                    <span class="tag">Ready to ship</span>
                    <h2 class="title-lg">Add njX UI and build the page.</h2>
                    <p>
                        A strong end-of-page block for docs, products and
                        launch pages.
                    </p>
                </div>
                <div class="rcta-actions">
                    <a class="btn btn-light btn-lg" href="#">Install now</a>
                    <a class="btn btn-outline-white btn-lg" href="#">Open docs</a>
                </div>
            </section>
        </div>
        <div class="lib-code-wrapper">
            <div class="lib-code-container">
                <div
                    class="lib-code"
                    data-raw='<section class="section">
  <div class="container card-gradient p-8">
    <span class="tag">Ready to ship</span>
    <h2 class="title-lg mt-3">Add njX UI and build the page.</h2>
    <p>A strong end-of-page block for docs, products and launch pages.</p>
    <div class="flex gap-3 mt-6">
      <a class="btn btn-light btn-lg" href="#">Install now</a>
      <a class="btn btn-outline-white btn-lg" href="#">Open docs</a>
    </div>
  </div>
</section>'
                >
                    <span class="c">&lt;!-- Gradient CTA block --&gt;</span>
                </div>
            </div>
        </div>
    </div>

    <div class="lib-component">
        <div class="lib-component-label">Newsletter CTA</div>
        <div class="lib-preview cta-block-preview">
            <section class="rcta rcta-news card-glass">
                <div>
                    <span class="tag tag-accent">Updates</span>
                    <h2 class="title-md">Get new components and examples.</h2>
                    <p class="text-muted">
                        A compact signup CTA for docs and blog pages.
                    </p>
                </div>
                <form class="rcta-form">
                    <input class="input" type="email" placeholder="you@example.com" />
                    <button class="btn btn-primary">Subscribe</button>
                </form>
            </section>
        </div>
        <div class="lib-code-wrapper">
            <div class="lib-code-container">
                <div
                    class="lib-code"
                    data-raw='<section class="section">
  <div class="container card-glass p-6">
    <span class="tag tag-accent">Updates</span>
    <h2 class="title-md mt-3">Get new components and examples.</h2>
    <form class="flex gap-3 mt-5">
      <input class="input" type="email" placeholder="you@example.com">
      <button class="btn btn-primary">Subscribe</button>
    </form>
  </div>
</section>'
                >
                    <span class="c">&lt;!-- Newsletter CTA block --&gt;</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .cta-block-preview {
        display: block;
        padding: 0;
        overflow: hidden;
        background: var(--сolor-dark);
    }

    .rcta {
        min-height: 300px;
        display: grid;
        grid-template-columns: minmax(0, 1fr) auto;
        gap: 24px;
        align-items: center;
        padding: clamp(26px, 5vw, 48px);
        border-radius: 0;
    }

    .rcta h2,
    .rcta p {
        margin: 14px 0 0;
        max-width: 680px;
    }

    .rcta-gradient p {
        color: color-mix(in srgb, #fff 76%, transparent);
        line-height: 1.6;
    }

    .rcta-actions,
    .rcta-form {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        justify-content: flex-end;
    }

    .rcta-form {
        min-width: min(420px, 100%);
    }

    .rcta-form .input {
        min-width: 240px;
    }

    @media (max-width: 780px) {
        .rcta {
            grid-template-columns: 1fr;
        }

        .rcta-actions,
        .rcta-form {
            justify-content: flex-start;
        }
    }

    @media (max-width: 520px) {
        .rcta-form,
        .rcta-form .input,
        .rcta-form button,
        .rcta-actions a {
            width: 100%;
        }
    }
</style>
