<section class="lib-section" id="contact-blocks">
    <div class="lib-section-title">✉ Contact Blocks</div>
    <div class="lib-section-desc">
        Ready contact sections with cards, forms and support links.
    </div>

    <div class="lib-component">
        <div class="lib-component-label">Contact Page Section</div>
        <div class="lib-preview contact-block-preview">
            <section class="rc rc-page">
                <div class="rc-copy">
                    <span class="tag tag-primary">Support</span>
                    <h2 class="title-lg">Tell us what you are building.</h2>
                    <p class="text-muted">
                        A full contact page block with channel cards and a
                        focused support form.
                    </p>
                    <div class="rc-channels">
                        <a class="card-glass" href="#"><strong>Email</strong><span>Private support</span></a>
                        <a class="card-glass" href="#"><strong>GitHub</strong><span>Issues and bugs</span></a>
                        <a class="card-glass" href="#"><strong>Telegram</strong><span>Quick ping</span></a>
                    </div>
                </div>

                <form class="card-glass rc-form">
                    <label>
                        Topic
                        <select class="input">
                            <option>Bug report</option>
                            <option>Integration help</option>
                            <option>Feature request</option>
                        </select>
                    </label>
                    <label>
                        Email
                        <input class="input" type="email" placeholder="you@example.com" />
                    </label>
                    <label class="rc-full">
                        Message
                        <textarea class="input" rows="5" placeholder="Describe your issue or question"></textarea>
                    </label>
                    <button class="btn btn-primary">Send message</button>
                </form>
            </section>
        </div>
        <div class="lib-code-wrapper">
            <div class="lib-code-container">
                <div
                    class="lib-code"
                    data-raw='<section class="section">
  <div class="container grid grid-2 gap-6">
    <div>
      <span class="tag tag-primary">Support</span>
      <h2 class="title-lg mt-3">Tell us what you are building.</h2>
      <p class="text-muted">A full contact page block with channel cards and a focused form.</p>
      <div class="grid grid-3 gap-3 mt-5">
        <a class="card-glass p-4" href="#">Email</a>
        <a class="card-glass p-4" href="#">GitHub</a>
        <a class="card-glass p-4" href="#">Telegram</a>
      </div>
    </div>
    <form class="card-glass p-5 grid gap-3">
      <input class="input" type="email" placeholder="you@example.com">
      <textarea class="input" rows="5" placeholder="Message"></textarea>
      <button class="btn btn-primary">Send message</button>
    </form>
  </div>
</section>'
                >
                    <span class="c">&lt;!-- Contact page block --&gt;</span>
                </div>
            </div>
        </div>
    </div>

    <div class="lib-component">
        <div class="lib-component-label">Compact Support Card</div>
        <div class="lib-preview contact-block-preview">
            <section class="rc rc-compact card-gradient">
                <div>
                    <span class="tag">Need help?</span>
                    <h2 class="title-md">Open an issue or send a message.</h2>
                    <p>Use this block at the bottom of docs and product pages.</p>
                </div>
                <div class="rc-actions">
                    <a class="btn btn-light" href="#">Email support</a>
                    <a class="btn btn-outline-white" href="#">GitHub issue</a>
                </div>
            </section>
        </div>
        <div class="lib-code-wrapper">
            <div class="lib-code-container">
                <div
                    class="lib-code"
                    data-raw='<section class="section">
  <div class="container card-gradient p-6">
    <span class="tag">Need help?</span>
    <h2 class="title-md mt-3">Open an issue or send a message.</h2>
    <div class="flex gap-3 mt-5">
      <a class="btn btn-light" href="#">Email support</a>
      <a class="btn btn-outline-white" href="#">GitHub issue</a>
    </div>
  </div>
</section>'
                >
                    <span class="c">&lt;!-- Compact support card --&gt;</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .contact-block-preview {
        display: block;
        padding: 0;
        overflow: hidden;
        background:
            radial-gradient(circle at 10% 0, color-mix(in srgb, var(--color-primary) 16%, transparent), transparent 26rem),
            var(--сolor-dark);
    }

    .rc {
        min-height: 420px;
        padding: clamp(24px, 5vw, 44px);
    }

    .rc-page {
        display: grid;
        grid-template-columns: minmax(0, 0.95fr) minmax(320px, 1.05fr);
        gap: 22px;
        align-items: start;
    }

    .rc-copy h2,
    .rc-copy p {
        margin: 14px 0 0;
    }

    .rc-copy p {
        line-height: 1.65;
    }

    .rc-channels {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 10px;
        margin-top: 22px;
    }

    .rc-channels a {
        display: grid;
        gap: 5px;
        min-height: 96px;
        align-content: center;
        padding: 14px;
        border-radius: var(--radius-lg);
        color: var(--color-light);
        text-decoration: none;
    }

    .rc-channels span {
        color: var(--color-muted);
        font-size: 13px;
    }

    .rc-form {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 14px;
        padding: 20px;
        border-radius: var(--radius-lg);
    }

    .rc-form label {
        display: grid;
        gap: 8px;
        color: var(--color-light);
        font-size: 13px;
        font-weight: 800;
    }

    .rc-full {
        grid-column: 1 / -1;
    }

    .rc-form button {
        width: fit-content;
    }

    .rc-compact {
        display: grid;
        grid-template-columns: minmax(0, 1fr) auto;
        gap: 22px;
        align-items: center;
        min-height: 300px;
    }

    .rc-compact h2,
    .rc-compact p {
        margin: 12px 0 0;
    }

    .rc-compact p {
        color: color-mix(in srgb, #fff 72%, transparent);
    }

    .rc-actions {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        justify-content: flex-end;
    }

    @media (max-width: 850px) {
        .rc-page,
        .rc-compact {
            grid-template-columns: 1fr;
        }

        .rc-channels,
        .rc-form {
            grid-template-columns: 1fr;
        }

        .rc-actions {
            justify-content: flex-start;
        }
    }
</style>
